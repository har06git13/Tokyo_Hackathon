# task-0310-11: 防災ヒント・空表示テキスト修正

## 概要
`timelineData.length === 0` 時（行動履歴なし）の「防災に向けてのヒント」セクションの
表示テキストを仕様書通りに修正する。

## 関連仕様
- 仕様書 §4.8.3-4 / §4.8.6 / §12.4.4-2

## 問題
| | 実装 | 仕様 |
|---|---|---|
| `timelineData.length === 0` 時の表示 | `あなたの行動をもとにアドバイスが表示されます` | `生存のヒントがありません` |

仕様 §12.4.4-2:「`timelineData.length === 0` の場合は `buildHints` を呼ばず空配列を返す（呼び出し元 ResultPage で制御）」
仕様 §4.8.6:「`生存のヒントがありません` を表示する」

## 変更ファイル
| ファイル | 種別 | 内容 |
|----------|------|------|
| `client/src/pages/ResultPage.jsx` | 修正 | `timelineData.length === 0` ブランチのテキストを修正 |

## 完了条件
- [x] 行動履歴が空の場合に `生存のヒントがありません` が表示される
- [x] `あなたの行動をもとにアドバイスが表示されます` は削除される

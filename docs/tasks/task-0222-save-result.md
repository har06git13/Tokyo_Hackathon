# Task 5: サーバーへの結果保存

> 仕様: [../result-page-spec.md](../result-page-spec.md) サーバー連携

## ステータス: ⬜ 未着手

## 対象ファイル

- `client/src/pages/ResultPage.jsx`

## 内容

- 初回レンダリング時に `POST /api/results` を呼び出し
- 二重送信防止（`useRef`）
- エラーハンドリング（失敗時はコンソール警告のみ）
- Atom の値（日本語文字列）をそのまま送信（キーへの変換はしない）

## レビューポイント

- 二重送信が確実に防止されているか
- StrictMode の二重レンダリングで問題が起きないか
- 送信データの形式が API 仕様と一致しているか
- Atom の値が日本語文字列のまま送信されているか（キー変換が混在していないか）

## 作業ログ

> （着手時に記録）

# Copy this file to `client/.env` and fill in values. Do NOT commit the real `.env`.
# Notes:
# - Any env var exposed to the browser must be prefixed with `REACT_APP_` (Create React App behavior).
# - Maps JS API key used on the client will be visible in the built site; restrict it by HTTP referrer in Google Cloud.

# Public (browser) variables - prefix with REACT_APP_
# Google Maps JavaScript API key (used in client-side map rendering)
REACT_APP_GOOGLE_MAPS_API_KEY=YOUR_CLIENT_SIDE_API_KEY

# Development convenience
# Enable HTTPS in dev to allow geolocation / camera APIs on some browsers (optional)
HTTPS=true

# Source maps in production builds can be disabled for smaller bundles / slightly more secrecy
GENERATE_SOURCEMAP=false

# Example:
# REACT_APP_GOOGLE_MAPS_API_KEY=AbCdEfGhIjKlMnOpQrStUvWxYz

# Copy this file to `server/.env` and fill in values. Do NOT commit the real `.env`.

# Server configuration
PORT=4000

# Server-side Google Maps API key (only if you call Google APIs from the server)
# Keep this private — do not expose to clients. Example use: geocoding, directions on the backend.
GOOGLE_MAPS_API_KEY=YOUR_SERVER_SIDE_API_KEY

# Example:
# GOOGLE_MAPS_API_KEY=AbCdEfGhIjKlMnOpQrStUvWxYz
